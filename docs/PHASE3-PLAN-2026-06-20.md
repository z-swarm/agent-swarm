# agent-swarm Phase 3 计划(W14-W21)

> **起草日期**: 2026-06-20
> **起草者**: Mavis
> **依据**: `docs/REVIEW-2026-06-19-2.md`(第二轮审查 11 风险点已闭环) + `DESIGN.md §15.3 / §17.2 / §17.6 / §16`
> **基准 commit**: `691e748`(H1 + M1-M3 + L1-L7 修复后)
> **v2 修订日期**: 2026-06-20(集成审查 4 条强制修订)
> **状态**: 已修订 + W14 启动中

## v2 修订摘要(基于独立审查 `REVIEW-2026-06-20-P3`)

| # | 修订 | 来源 |
|---|------|------|
| 1 | **W14 拆 2 周**:W14a (MCP SSE/重连/熔断/G-018, 1 周) + W14b (CI/concepts/troubleshooting/doctor, 1 周) | 审查建议 1 |
| 2 | **W19 Docker 默认模式保守化**:保留 `WORKSPACE_ONLY` 默认,`DOCKER` opt-in,`doctor` 建议生产切 DOCKER;不打 breaking | 审查建议 2 |
| 3 | **W20 MCP source 分级收紧**:`official → LOW` / `community → HIGH` / `private → MEDIUM`;`source` 字段强制必填 | 审查建议 3 |
| 4 | **每周 DoD 加 §17.1 三件套**:每周末 `git tag w<N>-demo` + `demos/wk<N>-<feature>.mp4` + 视频 ≤120s 写入 DoD ⑨ 校验 | 审查建议 4 |
| 5 | **W17 §16.3 #11 lint 分两阶段**:前 2 天 audit + 改 9 处用法,后 3 天上 lint 规则(只对 `src/agent_swarm/` 生效) | 审查建议 5 |
| 6 | **W21 release 任务前移到 W20 末**:CHANGELOG 草稿 + 0.3.0-rc1 + PyPI test 提前;W21 专做复测+复盘+正式 tag | 审查建议 6 |

> v2 后总周数 9 周(原 8 周),W14 拆 2 周;整体符合 §17.1 强制规则 + §16.2 倾向

---

## 0. 拆分原则

对标 Phase 1(W1-W6)/ Phase 2(W7-W12 + W13 Dogfooding)节奏:

- **每周一个垂直切片**,周末必须有可演示产出
- **每周末必须 `git tag w<N>-demo`**
- **每周末必须录制 ≤2 分钟演示视频**
- **当周 DoD 未通过 → 下周计划顺延**(§17.1 强制规则)
- **不允许"假桩"**(mock 留待下周 = 风险后置)

时间预算:Phase 1 6 周 / Phase 2 7 周(实际跨度 10 天),Phase 3 v2 估 9 周(W14 拆 2 周)。

---

## 1. Phase 3 整体节奏(v2 修订:9 周)

| 时段 | 主题 | 周次 |
|---|---|---|
| **第 1 周** | MCP SSE / 重连 / 熔断 / G-018 | **W14a** |
| **第 2 周** | CI 落地 + concepts/troubleshooting/doctor | **W14b** |
| **第 3 周** | 可观测加固 + DX 工具 | W15 |
| **第 4-5 周** | 多租户开放(核心 DoD) | W16-W17 |
| **第 6 周** | Redis 后端 | W18 |
| **第 7 周** | Docker Sandbox(保守版:WORKSPACE_ONLY 默认) | W19 |
| **第 8 周** | 密钥管理 + release 草稿 | W20 |
| **第 9 周** | 收尾 + 0.3.0 正式发布 | W21 |

---

## 2. 详细 Weekly Slice DoD

> 对标 `DESIGN.md §17.2` Phase 2 表格格式。每项 ①...⑧ 都是**机器可验证**的完成条件。

### W14a — MCP SSE / 重连 / 熔断 / G-018(v2 拆出:核心 1 周)

| # | 自动化 DoD(CI 必须全绿) |
|---|---|
| ① | `pytest tests/unit/test_mcp_sse.py tests/unit/test_mcp_resilience.py tests/unit/test_mcp_circuit_breaker.py` 全过 |
| ② | `StdioMCPClient` / `SseMCPClient` 共享 `BaseMCPClient`(消除重复代码);SSE 走真 HTTP(用 `aiohttp_client` mock) |
| ③ | `MCPRegistry` 启动时 connect + health check;`reliability.auto_reconnect=True` 真生效(测试:server 启动后 kill 重启,client 自动重连) |
| ④ | Circuit breaker 阈值触发:`max_consecutive_failures >= 3` → 熔断 60s → 后续调用立即返回 `MCPCircuitOpenError`,不阻塞;`tools/count_reconnect.py` 验证 |
| ⑤ | Golden Case **G-018**(MCP server 崩溃自动重连 3 次后熔断)从"Phase 2 计划"提前到 W14a,作为本周末 DoD |
| ⑥ | `examples/w14a_mcp_resilience.yaml` 演示:启动 swarm → 手动 kill MCP server → 客户端 3 次重连后熔断 → TUI 实时显示熔断状态 |
| ⑨ | `git tag w14a-demo` 已存在 + `demos/wk14a-mcp-resilience.mp4` ≤120s 存在(§17.1 三件套) |

**周末演示**: `agent-swarm run examples/w14a_mcp_resilience.yaml` 启动 → 手动 kill MCP server → 客户端 3 次重连后熔断 → TUI 实时显示熔断状态

### W14b — CI 落地 + 文档 + doctor(v2 拆出:基础设施 1 周)

| # | 自动化 DoD |
|---|---|
| ① | `.github/workflows/ci.yml` 增补:`fast-checks`(lint + unit + integration, < 5min) + `security-scan`(每 PR 跑 `tools/agent_review.py`);`nightly.yml` 加 `golden-replay` 完整跑 |
| ② | `docs/concepts.md` 落地:5 章节(Agent / Task / Mailbox / KB / AdversarialVerifier),每章 ≤300 字 + 1 张图 |
| ③ | `docs/troubleshooting.md` 落地:≥10 个常见错误 + Golden Case 链接 + `agent-swarm doctor` 集成 |
| ④ | `agent-swarm doctor` 子命令:LLM provider 连通 / SQLite 锁 / MCP server 状态 / 密钥就位 4 类检查 |
| ⑤ | CI 加 `windows-latest` runner 矩阵(对应 §17.6 TUI Windows 兼容风险,缓解而非治愈) |
| ⑨ | `git tag w14b-demo` + `demos/wk14b-ci-pipeline.mp4` 录屏(展示 CI 触发 + 3 个 job 跑通) |

---

### W15 — 可观测加固 + DX 工具

| # | DoD |
|---|---|
| ① | `pytest tests/unit/test_prometheus_sink.py tests/integration/test_metrics_pipeline.py` 全过 |
| ② | `PrometheusSink` 注册到 `ObservabilityBus`;暴露 `GET /metrics` 端点(`prometheus_client.start_http_server` 或 aiohttp 集成) |
| ③ | 关键指标埋点:`framework_tasks_total` / `framework_llm_tokens_total` / `framework_cas_conflict_total` / `framework_mcp_circuit_state` / `framework_approval_pending_count` |
| ④ | `docs/grafana/dashboard.json` 落地:≥4 面板(Task throughput / Token 消耗 / CAS 冲突率 / MCP 熔断状态),导入即用 |
| ⑤ | `tools/agent_review.py` 加 `--require-human-review` flag:开启时,任何 CRITICAL 级别 finding 必须有人 `--approve-override` 才放行;缺失时 exit 2 |
| ⑥ | `docs/recipes/` 落地 ≥5 个常见任务:`pr_review.md` / `debug_production.md` / `docs_generation.md` / `migration.md` / `security_audit.md`,每个含可复制粘贴的 YAML |
| ⑦ | Golden Case **G-019** 同步前置(部分):新增"agent_review 识别真问题"子 case 跑通 |
| ⑧ | §17.6 风险登记表 review:本月新增/淘汰项 PR 合并,文件 `docs/RISK-LOG.md` 落地 |
| ⑨ | `git tag w15-demo` + `demos/wk15-metrics-dashboard.mp4` ≤120s 存在 |

**周末演示**: `agent-swarm run examples/w15_metrics.yaml` 启动 → 打开 `localhost:9090/graph` 查 4 面板 → 触发 MCP 熔断 → 熔断状态指标实时变化

---

### W16 — 多租户开放(上)

| # | DoD |
|---|---|
| ① | `pytest tests/unit/test_tenant_quota.py tests/unit/test_multi_tenant_config.py tests/integration/test_multi_tenant_isolation.py` 全过 |
| ② | `SecurityContext` 配置加 `mode: single \| multi` 字段;`multi` 模式下 `tenant_id` 必填且非默认值 |
| ③ | `TenantQuota` 落地:`max_agents` / `max_concurrent_tasks` / `max_tokens_per_hour`;超限抛 `TenantQuotaExceeded` + emit `quota.exceeded` 事件 |
| ④ | SQL 全量审计:`tools/check_sql_lint.py` 用 grep -E 检测每条 SQL 含 `WHERE tenant_id = ?`;CI 阻塞 |
| ⑤ | `KnowledgeBase` / `Mailbox` / `TaskQueue` 全部走 `tenant_id` 自动注入(contextvars);`tests/security/test_no_explicit_tenant.py` 静态扫描 src/ 下显式 `tenant_id=` 调用(非 ctx 工厂位置)退出非 0 |
| ⑥ | 跨租户攻击套件:`tests/security/test_cross_tenant.py` 50 条场景(读 / 写 / 删 / 列举);CI 必跑 |
| ⑦ | `examples/w16_multi_tenant.yaml` 演示 2 个 tenant 并发跑,数据互不可见 |
| ⑧ | 设计文档:§8.4 多租户章节更新为"已落地",移出"待办"清单 |
| ⑨ | `git tag w16-demo` + `demos/wk16-multi-tenant.mp4` ≤120s 存在 |

**周末演示**: 启动 2 个 tenant 的 swarm → tenant A 试图读 tenant B 的 task → 被 `KnowledgeBase` 拦截 + emit `security.cross_tenant_attempt` 事件

---

### W17 — 多租户开放(下) + 跨通道 Session 合并

| # | DoD |
|---|---|
| ① | `pytest tests/e2e/test_multi_tenant_load.py tests/integration/test_session_binding.py` 全过 |
| ② | 100 并发跨租户压测:`pytest tests/e2e/test_multi_tenant_load.py --workers=10 -n 10`;0 数据越权 + p99 延迟 ≤500ms |
| ③ | `SessionBindingManager` 支持同身份跨通道绑定:用户在飞书 @bot + CLI `agent-swarm run` 用同一 `user_id` → 共享 swarm session |
| ④ | 通道身份合并:飞书 `open_id` ↔ CLI `user_id` 通过 `tenant_id + identity_key` 映射;映射表走 SQLite |
| ⑤ | Golden Case **G-019**(100 并发跨租户读写 0 越权)本周末必须全过 |
| ⑥ | Phase 3 DoD ①(多租户压测)完成;`docs/MULTI-TENANT-REPORT.md` 记录压测结果 |
| ⑦ | `examples/w17_cross_channel.yaml` 演示飞书用户和 CLI 用户共同操作同一 swarm |
| ⑧ | §16.3 #11 闭环分两阶段:**W17a**(前 2 天)`tools/audit_create_task.py` 扫 + 改 9 处 `asyncio.create_task` 用法(websocket_sink:136/233, swarm:352/358, tui/app:306/309/310/465, mcp/client:171/177)→ 全部加 `context=ctx_var`;**W17b**(后 3 天)lint 规则 `no-bare-asyncio-create-task` 启用,只对 `src/agent_swarm/` 生效(允许带 `context=` kwarg) |
| ⑨ | `git tag w17-demo` + `demos/wk17-cross-channel.mp4` ≤120s 存在 |

**周末演示**: 同一用户在飞书发指令 + CLI 启 session → 跨通道 Session 合并 + 跨租户压测 0 越权

---

### W18 — Redis 后端

| # | DoD |
|---|---|
| ① | `pytest tests/unit/test_redis_backend.py tests/integration/test_redis_cas.py tests/integration/test_redis_distributed.py` 全过 |
| ② | `RedisStorageBackend` 实现 `StorageBackend` ABC(WATCH/MULTI/EXEC 实现 CAS,失败重试 ≤3 次) |
| ③ | `StorageBackend` 配置切换:`storage.backend: redis` + `storage.url: redis://...`;文档写明何时用 SQLite 何时用 Redis |
| ④ | 全部 Phase 1 Golden Case(G-001..G-010)在 Redis 后端跑通,无 regression |
| ⑤ | Golden Case **G-020**(10 agent + Redis 后端)首次通过 |
| ⑥ | `tools/bench_storage.py` SQLite vs Redis 性能对比:Redis 在 10+ agent 下吞吐 ≥ SQLite ×2 |
| ⑦ | §17.6 SQLite WAL 在容器/NFS 不可靠的应对:`storage.backend: redis` 自动启用;`agent-swarm doctor` 检测 SQLite 锁情况并告警 |
| ⑧ | Phase 3 DoD ②(Redis 通过全部 Phase 1 测试)完成 |
| ⑨ | `git tag w18-demo` + `demos/wk18-redis-throughput.mp4` ≤120s 存在 |

**周末演示**: 启动 10 agent swarm 跑 Redis 后端 → CAS 冲突可视化 + 性能基线对比图

---

### W19 — Docker Sandbox(**v2 保守化**:WORKSPACE_ONLY 默认,opt-in DOCKER)

| # | DoD |
|---|---|
| ① | `pytest tests/unit/test_docker_sandbox.py tests/security/test_docker_isolation.py tests/integration/test_docker_execution.py` 全过 |
| ② | `DockerSandboxManager` 实现 `SandboxManager` ABC;通过 docker SDK 启容器 + bind workspace 目录 |
| ③ | 容器配置:CIS Docker Benchmark 关键项(非 root 用户 / 限制 capabilities / 只读文件系统 / no-new-privileges) |
| ④ | **保守版**:新增 `SandboxMode.DOCKER`;**默认保持 `SandboxMode.WORKSPACE_ONLY`**(向后兼容,不打 breaking);`agent-swarm doctor` 检测 Docker 可用性并建议生产切 DOCKER;`§16.2 #1` 闭环方式改为"提供 DOCKER + 文档化推荐路径" |
| ⑤ | 攻击套件扩展:容器逃逸尝试(mount / privileged / network access)全部拦截;`tests/security/test_container_escape.py` 20 条 |
| ⑥ | `examples/w19_docker_sandbox.yaml` 演示显式 opt-in:`sandbox.mode: docker` 配置字段走 Docker;workspace 目录挂载 / 网络默认禁止 / 超时 kill |
| ⑦ | `tools/bench_sandbox.py` workspace_only vs Docker 性能对比;Docker 启动开销 ≤500ms(本地镜像) |
| ⑧ | Phase 3 DoD ④(Docker 通过 CIS Docker Benchmark)完成 |
| ⑨ | `git tag w19-demo` + `demos/wk19-docker-sandbox.mp4` ≤120s 存在 |

**周末演示**: agent 调用 `run_command` → 显式配置 `sandbox.mode: docker` → 走 Docker 容器 → 容器外看不到任何进程 / 文件系统;攻击套件 20 条全拦截;对比默认 `WORKSPACE_ONLY` 行为不变

---

### W20 — 密钥管理 + Release 草稿前移

| # | DoD |
|---|---|
| ① | `pytest tests/unit/test_secret_manager.py tests/integration/test_vault_rotation.py` 全过 |
| ② | `SecretManager` ABC 真实实现:`EnvSecretManager`(Phase 1 默认)+ `VaultSecretManager`(Phase 3 新增) |
| ③ | Vault 集成:AppRole 认证 + KV v2 secret engine;读 secret 走 `vault kv get` + 内存缓存 TTL |
| ④ | 凭证轮换通知:Vault token 即将过期(剩 ≤7 天)→ emit `secret.rotation_due` 事件 + 飞书卡片通知(走 W10 通道) |
| ⑤ | `examples/w20_vault.yaml` 演示 LLM API key 走 Vault;轮换测试 5 分钟内生效 |
| ⑥ | 全部 5 个 Phase 3 模块的 `--no-vault` / `--no-redis` / `--no-docker` 降级路径都通(允许在受限环境跑) |
| ⑦ | `docs/production-deployment.md` 落地:Docker compose / k8s manifest / systemd unit / 监控接入 4 套模板 |
| ⑧ | **v2 收紧**:§16.3 #10 闭环——`MCPServerConfig` 加 `source: official\|community\|private` 字段(强制必填,YAML 缺此字段启动失败);**默认分级**:`official → LOW` / `community → HIGH` / `private → MEDIUM`;MEDIUM 以上触发 Phase 2 W11 Approval 流程 |
| ⑨ | `git tag w20-demo` + `demos/wk20-vault-rotation.mp4` ≤120s 存在 |

**Release 草稿前移(v2 修订 6)**:
- W20 末 1-2 天:`docs/CHANGELOG.md` 草稿 0.2.0 → 0.3.0 + 0.3.0-rc1 git tag + PyPI test 发布
- 撞车缓冲:即使 W21 顺延 2-3 天,release 流程已就位

**周末演示**: LLM API key 走 Vault → 轮换 token → 5 分钟内 agent 无感切换;MCP source 字段缺省启动失败;生产部署文档走通 4 套模板

---

### W21 — Phase 3 收尾 + 0.3.0 发布(v2:release 任务前移,本周末专做复测+复盘+正式 tag)

| # | DoD |
|---|---|
| ① | 全部 W14-W20 DoD 复测 + 全量测试 1 次跑通(预计 670+ tests 真实值,vs 计划"1000+" 估算) |
| ② | Phase 3 全部 DoD 完成(见 §3) |
| ③ | `tests/golden/baseline.yaml` 更新(人工签字),对比 **W12 末尾基线**(不是 W14 开始),差异 +x% 内方可通过 |
| ④ | `docs/CHANGELOG.md` 0.2.0 → 0.3.0 升级说明(API 锁定 / breaking changes / migration 指南);W20 末已出草稿,本周末定稿 |
| ⑤ | `agent-swarm doctor` 集成所有 5 个新模块的健康检查(Vault / Redis / Docker / Prometheus / 多租户) |
| ⑥ | Phase 3 收尾复盘:`docs/PHASE3-RETRO.md`(做对的 / 做错的 / 留给 Phase 4 的) |
| ⑦ | 0.3.0 正式 git tag + PyPI 发布 + GitHub Release 笔记(W20 末 rc1 已发布,本周末 stable) |
| ⑧ | §16.2 仍开放项 review:#2 GUI 框架(定 React / HTMX) / #4 技能市场 / #5 多语言 SDK 推到 Phase 4/5 |
| ⑨ | `git tag w21-demo` + `demos/wk21-phase3-final.mp4` ≤180s 存在(15-20 分钟全特性演示的精简版) |

**周末演示**: 完整 Phase 3 演示(15-20 分钟)→ 包含多租户 / Redis / Docker / Vault / Prometheus 5 大新特性;精剪 ≤3 分钟存 `demos/wk21-phase3-final.mp4`

---

## 3. Phase 3 整体 DoD(对应 `DESIGN.md §17.2` Phase 级别)

| # | 自动化 DoD | 完成周次 |
|---|---|---|
| ① | 多租户压测 100 并发跨租户 0 越权 | W17 |
| ② | Redis 后端通过全部 Phase 1 测试 | W18 |
| ③ | Prometheus + Grafana 看板模板交付 | W15 |
| ④ | Docker sandbox 通过 CIS Docker Benchmark 关键项 | W19 |
| ⑤ | (新增)Vault 凭证轮换 5 分钟内生效 | W20 |
| ⑥ | (新增)MCP SSE + 重连 + 熔断全链路通过 | W14 |
| ⑦ | (新增)1000+ 测试全过,mypy 0 errors,coverage ≥85% | W21 |

---

## 4. 与 DESIGN 现有内容的对应关系

### 4.1 覆盖的 DESIGN §15.3 模块

| Phase 3 模块 | 对应周次 | 备注 |
|---|---|---|
| 多租户开放 | W16-W17 | 拆 2 周(配置 + 压测) |
| Docker Sandbox | W19 | + §16.2 #1 闭环 |
| Redis 后端 | W18 | 拆 1 周 + §17.6 SQLite WAL 应对 |
| Prometheus 导出 | W15 | 提前(可观测是其它特性的依赖) |
| 密钥管理 | W20 | + §16.3 #10 闭环 |

### 4.2 覆盖的开放问题(§16)

| 编号 | 闭环周次 |
|---|---|
| §16.2 #1 Sandbox 默认模式 | W19 |
| §16.2 #2 GUI 框架(推 Phase 4) | W21 决定 |
| §16.3 #9 AdversarialVerifier ELIMINATE_THRESHOLD 调参 | (Phase 2 末期 G-013 监控中,Phase 3 收尾 review) |
| §16.3 #10 MCP ToolRisk 按 server 来源分级 | W20 |
| §16.3 #11 SecurityContext 在 `asyncio.create_task` 中丢失 | W17 |

### 4.3 应对的风险登记表(§17.6)

| 风险 | 应对周次 | 缓解措施 |
|---|---|---|
| MCP 协议变更 | W14 | SDK 版本锁定 + 适配层隔离(G-018 监控) |
| 飞书 API 改版 | (持续) | 卡片模板版本化 + LarkConnector 可替换 |
| LLM 限流突变 | (Phase 2 G-010 已有) | Phase 3 W15 Prometheus 埋点 + Grafana 报警 |
| AdversarialVerifier 不收敛 | (持续) | G-013 监控 + §16.3 #9 调参 |
| SQLite WAL 在容器/NFS 不可靠 | W18 | Redis 自动启用 + `doctor` 告警 |
| Agent 死循环烧 token | (Phase 1 W5) | TokenBudget + W15 指标埋点 |
| Prompt injection 越权 | (Phase 1 W5) | SecurityPolicy + W15 G-006/G-007 持续 |
| LLM 输出 schema 漂移 | (持续) | Provider retry + JSON Schema 强制 |
| 测试 LLM 真实调用成本失控 | W15 | Prometheus 成本指标 + 阈值报警 |
| TUI Windows 兼容性 | W14-W15 | (新增)GitHub Actions Windows runner 跑 TUI 烟测 |

---

## 5. 时间线总览(v2:9 周)

```
2026-06-19  691e748 H1+修复完成  ← 当前 HEAD
2026-06-20  PHASE3-PLAN-v2 修订落地(本文档)
2026-06-20  **W14a 启动**(MCP SSE + 重连 + 熔断 + G-018)
2026-06-27  W14a demo tag + 录屏
2026-07-04  **W14b**(CI 落地 + concepts/troubleshooting/doctor + Windows runner)
2026-07-11  W14b demo tag
2026-07-18  W15(Prometheus + DX)
2026-07-25  W16(多租户 配置)
2026-08-01  W17(多租户 压测 + 跨通道 + §16.3 #11 闭环)
2026-08-08  W18(Redis)
2026-08-15  W19(Docker 保守版:WORKSPACE_ONLY 默认)
2026-08-22  W20(Vault + MCP source 分级 + release 草稿)
2026-08-29  W21(收尾 + 0.3.0 正式发布)
```

---

## 6. 风险与开放决策

### 6.1 计划内风险

| 风险 | 概率 | 应对 |
|---|---|---|
| W14 MCP SSE + 熔断实现比预期复杂(2-3 天) | 中 | 允许 W15 Prometheus 顺延 1 周 |
| W16-W17 多租户需要重构 contextvars 流 | 中 | §16.3 #11 闭环 = 重构成本,接受 |
| W18 Redis CAS 性能调优耗时 | 中 | `bench_storage.py` 提前跑,有问题及时调整设计 |
| W19 Docker SDK 在 Windows 兼容性 | 高 | W19 期间用 Linux 优先,Windows 延后;不影响主线 |
| W20 Vault 集成需要先有 Vault server | 低 | 用 Vault dev mode 测,生产部署文档化 |

### 6.2 待决策项(W21 前需确定)

- §16.2 #2 GUI 框架:React / HTMX / Alpine.js / Vue?
- 0.3.0 是否引入 breaking changes(API 锁定 vs 继续 0.x)?
- Phase 4 优先级:Web GUI Dashboard vs Pipeline Engine vs 技能市场?

---

## 7. 建议的评审顺序

1. **本计划 vs DESIGN §15.3 5 项**:是否完整覆盖?(预计 ✅)
2. **W14 技术债清单**:MCP SSE / 重连 / 熔断 / CI / docs 起头——负担是否过重?
3. **W16-W17 拆 2 周**:多租户是否值得拆细?
4. **§16.2 #1 Sandbox 默认切 Docker(W19)**:是否过于激进?可以保留 `WORKSPACE_ONLY` 默认,`DOCKER` opt-in
5. **W17 §16.3 #11 闭环**:lint 规则强制 `asyncio.create_task` 包装,可能影响外部代码兼容性
6. **W20 §16.3 #10 MCP server source 分级**:默认 `community → MEDIUM` 是否安全?还是 `community → HIGH`?

---

## 8. 附录:关键数字

| 维度 | Phase 1 实际 | Phase 2 实际 | Phase 3 v1 估算 | **Phase 3 v2 目标(实事求是)** |
|---|---|---|---|---|
| 周数 | 6 | 7 | 8 | **9** |
| 新增模块 | 10 | 12 | ~15 | **~12**(W14 拆细,合并类似项) |
| 新增测试 | ~250 | ~400 | ~300 | **~250** |
| 测试总数 | ~280 | ~680 | ~1000 | **~930**(670 门禁 + ~250 新增) |
| 覆盖率 | 75% → 93% | 93% → ? | ≥85% | **≥85%** |
| src 行数 | 5500 | 8000 | ~12 000 | **~10 500** |
| commit 数 | 18 | 21 | ~24 | **~26** |

---

## 9. 相关文档索引

- 初轮审查:`docs/REVIEW-2026-06-19.md`
- 第二轮审查(含 H1):`docs/REVIEW-2026-06-19-2.md`
- 第二轮审查闭环:`docs/REVIEW-2026-06-19-2-closure.md`
- 设计文档:`DESIGN.md`(§15 / §17.2 / §17.6 / §16 重点参考)
- Phase 1/2 DoD:`DESIGN.md §17.2` Phase 1 / Phase 2 表
